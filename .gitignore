# macOS
.DS_Store

# Xcode build artefacts
build/
DerivedData/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

# Xcode user-specific settings (breakpoints, open tabs, window layout)
xcuserdata/
*.xcuserstate
*.xccheckout
*.moved-aside

# Fastlane (if added later)
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# CocoaPods (if added later)
Pods/
Podfile.lock

# Swift Package Manager (if added later)
.build/
.swiftpm/

# Sensitive / environment-specific
*.env
Secrets.swift

# Claude Code project memory (local only)
.claude/
